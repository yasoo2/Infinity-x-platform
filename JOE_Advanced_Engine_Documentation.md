'''
# 🚀 توثيق JOE Advanced Engine - القدرات المتقدمة

**تاريخ التحديث:** 2025-11-04

**المؤلف:** Manus AI

## 1. مقدمة

تم تحديث JOE بقدرات ذكاء اصطناعي متقدمة، مما يجعله أقرب إلى قدرات Manus AI. المحرك الجديد، **JOE Advanced Engine**، يعتمد على نظام **Function Calling** الذي يسمح لـ JOE باستخدام أدوات خارجية بشكل تلقائي لتنفيذ مهام معقدة.

هذا التوثيق يشرح القدرات الجديدة بالتفصيل وكيفية عملها.

## 2. نظام Function Calling

نظام Function Calling هو جوهر التحديث الجديد. بدلاً من مجرد الرد على النصوص، يمكن لـ JOE الآن فهم الأهداف، واختيار الأدوات المناسبة، وتنفيذها للحصول على نتائج حقيقية. هذا يفتح الباب أمام إمكانيات لا حصر لها، مثل:

- البحث في الإنترنت للحصول على معلومات محدثة.
- تصفح مواقع الويب واستخراج البيانات منها.
- بناء مشاريع برمجية كاملة.
- التفاعل مع واجهات برمجية (APIs) خارجية.

### آلية العمل

1.  **فهم الهدف:** يقوم JOE بتحليل رسالة المستخدم لفهم الهدف الرئيسي.
2.  **اختيار الأداة:** بناءً على الهدف، يختار JOE الأداة (أو مجموعة الأدوات) الأنسب من قائمته المتاحة.
3.  **تحديد المعلمات:** يقوم JOE باستخراج المعلمات المطلوبة لتشغيل الأداة من رسالة المستخدم.
4.  **تنفيذ الأداة:** يتم استدعاء الدالة البرمجية الخاصة بالأداة مع المعلمات المحددة.
5.  **الحصول على النتيجة:** يتم إرجاع نتيجة تنفيذ الأداة إلى JOE.
6.  **صياغة الرد:** يستخدم JOE النتيجة لصياغة رد نهائي للمستخدم، مع شرح ما تم إنجازه.

## 3. الأدوات المتاحة

فيما يلي قائمة بجميع الأدوات المتاحة حاليًا في JOE Advanced Engine:

| الأداة (Function Name)      | الوصف                                                    | المعلمات (Parameters)                                                                                               |
| --------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `search_web`                | البحث في الإنترنت عن معلومات محدثة.                      | `query` (string): استعلام البحث.                                                                                    |
| `get_weather`               | الحصول على معلومات الطقس الحالية لمدينة معينة.          | `city` (string): اسم المدينة.                                                                                       |
| `browse_website`            | تصفح موقع ويب وجمع المعلومات الأساسية منه.             | `url` (string): رابط الموقع المراد تصفحه.                                                                           |
| `extract_info_from_url`     | استخراج معلومات محددة من صفحة ويب.                      | `url` (string): رابط الموقع, `query` (string): المعلومات المطلوب البحث عنها.                                       |
| `build_website`             | بناء مشروع موقع ويب كامل من الصفر.                       | `projectType` (enum), `description` (string), `style` (enum), `features` (array).                                   |

### أمثلة على الاستخدام

- **للبحث عن معلومات:**
  > "ابحث عن آخر أخبار الذكاء الاصطناعي"
  *سيستخدم JOE أداة `search_web` ويعرض لك النتائج.*

- **لمعرفة الطقس:**
  > "ما هو طقس الرياض اليوم؟"
  *سيستخدم JOE أداة `get_weather` ويعطيك حالة الطقس بالتفصيل.*

- **لتصفح موقع:**
  > "تصفح موقع x.ai واعرض لي المعلومات الأساسية"
  *سيستخدم JOE أداة `browse_website` ويستخرج لك عنوان الموقع ووصفه وأهم العناوين.*

- **لبناء موقع:**
  > "ابني لي موقع شخصي لعرض أعمالي، بأسلوب عصري ومميزات تفاعلية"
  *سيستخدم JOE أداة `build_website` وينشئ لك ملفات المشروع كاملة.*

## 4. التفاصيل التقنية

- **Backend:** تم بناء المحرك المتقدم باستخدام Node.js و Express.
- **AI Model:** يتم استخدام `gpt-4o-mini` من OpenAI لدعم نظام Function Calling.
- **API Endpoint:** تم إضافة Endpoint جديد للتعامل مع الطلبات المتقدمة:
  `POST /api/v1/joe/chat-advanced`
- **Frontend:** تم تحديث الواجهة الأمامية (React) لاستخدام الـ Endpoint الجديد وعرض الأدوات المستخدمة في الرد.

## 5. الخلاصة

JOE الآن ليس مجرد مساعد دردشة، بل هو مساعد ذكي قادر على تنفيذ مهام حقيقية. هذه القدرات الجديدة تفتح آفاقاً واسعة للتطوير المستقبلي، وستمكن JOE من أن يصبح أداة لا غنى عنها في منصة Infinity-X.

---

*هذا التوثيق تم إنشاؤه بواسطة Manus AI.*
'''
