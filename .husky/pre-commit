#!/bin/sh
branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" != "main" ]; then
  echo "[husky] رفض الالتزام: الفرع الحالي '$branch' ليس main"
  exit 1
fi
echo "[husky] الالتزام مسموح على main"
